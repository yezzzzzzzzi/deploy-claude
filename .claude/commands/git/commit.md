# git commit
현재 변경사항을 분석한 뒤, 아래 규칙에 따라 Git Commit을 수행한다.

## 작업 절차
1. `git status` 및 Staged/Unstaged 변경사항을 분석한다.
2. 변경 내용을 기반으로 적절한 Commit Prefix를 선택한다.
3. 아래 형식으로 Commit Message $ARUGUMENTS로 붙인다.
4. Commit을 실행한다.